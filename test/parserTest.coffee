chai = require "chai"
expect = chai.expect
assert = chai.assert

parser = require "../lib/parser"

describe('parse Node error', () ->
  it('should follow weasel standards', (done) ->
    assert.equal("", "")
    done()
  )
)